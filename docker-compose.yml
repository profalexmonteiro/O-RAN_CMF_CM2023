services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oran-cmf-web
    ports:
      - "80:8000"
    environment:
      PYTHONUNBUFFERED: "1"
      MPLBACKEND: Agg
    volumes:
      - .:/app
    restart: unless-stopped
